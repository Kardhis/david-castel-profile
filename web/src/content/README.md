# Contenido (fase posterior a la V1)

Aquí puedes añadir más adelante:

- artículos del blog (MDX o CMS headless),
- casos de estudio en Markdown,
- datos estructurados por entrada.

La V1 usa textos en `src/messages/*.json` y secciones en `src/components/`.
